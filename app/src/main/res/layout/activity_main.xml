<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.chana.myapplication.MainActivity">


    <Button
        android:text="Enter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/Enter"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="185dp"
        android:onClick="nextpage"
        />

    <RadioGroup
        android:id="@+id/RG"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/Enter"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="17dp">

        <RadioButton
            android:text="Patient"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="28dp"
            android:id="@+id/PatientradioButton"
            android:layout_above="@+id/Enter"
            android:layout_alignRight="@+id/Enter"
            android:layout_alignEnd="@+id/Enter"
            android:layout_marginRight="42dp"
            android:layout_marginEnd="42dp"
            />

        <RadioButton
            android:text="Doctor"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:id="@+id/DoctorradioButton"
            android:layout_above="@+id/PatientradioButton"
            android:layout_alignLeft="@+id/PatientradioButton"
            android:layout_alignStart="@+id/PatientradioButton"
            />
    </RadioGroup>

</RelativeLayout>
